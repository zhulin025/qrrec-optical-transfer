# QRREC × RaptorQR 技术评估与整合规划

日期：2026-08-03

## 1. 结论摘要

RaptorQR 值得吸收，但不建议用它直接替换 QRREC 当前的 libcimbar 彩色矩阵路线。

- RaptorQR README 报告的 iPhone 16 / Safari 实验室结果为：95.2 KB 文件约 375 ms、约 254.0 KB/s；6.5 MB 文件约 36 s、约 183.6 KB/s。
- RaptorQR 的合成全链路基准（V30-L、4 QR、30 FPS、20% repair）计划传输速率为 167.58 KiB/s，解析器可达到 106.20 个合成显示帧/秒、354 QR/s。这个结果证明桌面解析余量，不等同于摄像头真机吞吐量。
- QRREC 当前最好的真机记录是 V5.2 B 模式 App 内显示 180 KB/s；V5.1 为 131.4 KB/s。它们按成功解码载荷统计，可能包含重复喷泉帧，不等于“原文件字节数 ÷ 从开始到完成的时间”。
- 因为两边测量口径不同，目前只能判断双方已经处于相近的百 KB/s 量级，不能据此宣称 RaptorQR 一定比 QRREC 快，或 QRREC 已经达到 180 KB/s 端到端净速率。

建议把整合目标定为：保留 V5.4 的 libcimbar/Wirehair 高密度彩色通道，新增一个独立的“V6 标准 QR 高速实验版”，先复用 RaptorQR 的 benchmark、RaptorQ、调度和多码并行方法，再由同一套真机测试决定是否进入主版本。原 V6.0 音频方案只保留为已停止的技术预研。

## 2. RaptorQR 的速率是怎样得到的

### 2.1 真机公开结果

| 场景 | RaptorQR 报告结果 | 口径 |
| --- | ---: | --- |
| V30-L × 4 QR @ 30 FPS，95.2 KB | 375 ms，约 254.0 KB/s | iPhone 16 / Safari，实验室真机完成时间 |
| V30-L × 4 QR @ 30 FPS，6.5 MB | 36 s，约 183.6 KB/s | iPhone 16 / Safari，实验室真机完成时间 |
| V20，4 QR @ 30 FPS | 最高 300 个 QR/s | 成功解码符号率，不是净文件速率 |
| V30，4 QR @ 30 FPS | 100+ 个 QR/s | 成功解码符号率，不是净文件速率 |

小文件的 254.0 KB/s 不应作为持续速率承诺。更有参考价值的是 6.5 MB 的 183.6 KB/s，但仍需在我们的设备、屏幕、距离和光照条件下复测。

### 2.2 合成 benchmark 的理论结构

RaptorQR 的 release gate 使用以下参数：

- QR：V30-L；单个 RaptorQ 有效载荷 1716 B。
- 并行度：每个显示帧 4 个二维码，2×2 排列。
- 显示速率：30 FPS，共 10 秒、300 个显示帧。
- 总符号槽：1200；其中接收 1000 个源符号，随机丢弃 200 个符号。
- 文件载荷：1,716,000 B。
- 计划净速率：`1,716,000 / 10 / 1024 = 167.58 KiB/s`。
- 未扣 repair 的物理槽位上限：`1716 × 4 × 30 / 1024 ≈ 201.1 KiB/s`。

因此它的速度主要来自“单码高版本大载荷 × 四码并行 × 30 FPS”，RaptorQ 的作用主要是用可控冗余抵抗漏扫，而不是凭空提高光学信道容量。

## 3. 最值得吸收的经验

### 3.1 统一端到端速率口径

RaptorQR 的接收页面用完整数据长度除以首次识别到完成的时间，方向是正确的。QRREC 应同时记录三类指标：

1. 光学解码载荷率：成功解码帧返回的字节数/秒，用于定位相机与解码瓶颈。
2. 唯一编码块接收率：去重后进入喷泉解码器的新块字节数/秒，用于观察丢帧和重复帧。
3. 端到端净文件速率：原文件大小 ÷（点击开始发送到接收端文件校验、解压完成），作为版本对比和对外描述的主指标。

同时记录首帧等待、编码/压缩、传输、重建/解压四段耗时，避免压缩后只报告光学部分。

### 3.2 RaptorQ WASM 与系统包优先

RaptorQR 使用 Rust `cberner/raptorq` 的 WASM 构建作为主 FEC，并保留系统源符号优先、repair 包补偿的语义。相比 QRREC V1–V4 的自研 LT 路径，它更适合大文件、可控 repair 和晚加入接收。

建议先作为 V6 独立协议验证，不修改 V1–V4 协议，也不替换 V5.x 的 Wirehair：

- 第一轮直接评估 `@raptorqr/core` / `@raptorqr/raptorq-wasm`，减少重复实现。
- 固定依赖版本并保存 WASM 校验值；离线构建必须可复现。
- 若包边界或体积不适合 QRREC，再只移植协议/FEC 适配层。
- RaptorQR 主仓库为 MIT；整合时仍需保留版权声明，并单独核对 RaptorQ、fast_qr、ZXing WASM 的上游许可证与制品来源。

### 3.3 多 QR 并行，而不是只追求更高 FPS

四码并行把每次完整屏幕曝光变成四个独立符号；当摄像头采到稳定帧时，收益比盲目提高动画 FPS 更确定。QRREC V2/V3 已有双 QR 经验，可扩展为统一的 1/2/4 码布局：

- 默认先做 2 QR 和 4 QR，不先做 6/8 QR；手机画面中单码像素不足时，高并行度会迅速降低识别率。
- 发送端显示实际 FPS 和 QR/s，不只显示目标 FPS。
- 接收端一次扫描允许返回多个符号，并对每个符号独立验 CRC、去重和送入 FEC。
- 以屏幕物理尺寸和接收画面中“每个 QR 的像素边长”建立自适应降档，而不是只按设备型号猜测。

### 3.4 源包与 repair 包的播放策略

RaptorQR 的 Balanced 策略首轮优先源包，后续循环把 repair 均匀穿插；多码布局只负责条带化，不改变包分类。这比简单按生成顺序循环更利于：

- 无明显丢帧时快速完成；
- 连续漏扫时不把所有修复能力集中到循环尾部；
- 接收端中途加入后，在一个循环内持续拿到有效修复包。

QRREC 可为 V6 实现三种可测试策略：source-first、balanced、even-spread，默认 balanced，并用固定随机种子保证 benchmark 可复现。

### 3.5 发送端预渲染窗口与 Worker 池

RaptorQR 将 FEC、QR 生成、显示分层，并按并行 QR 数建立渲染 Worker 池和有限缓存窗口。QRREC 可吸收：

- 编码/FEC 与 QR 光栅化分 Worker；主线程只合成和显示。
- 不一次缓存整个大文件的全部二维码；只预渲染未来若干显示帧。
- 缓存不足时降 FPS，不显示未完成或撕裂的二维码帧。
- 将实际渲染 FPS、缓存水位、跳帧数纳入发送端 telemetry。

### 3.6 建立三层基准，而不是只看一次真机峰值

1. Codec benchmark：RaptorQ 编码/解码、内存峰值、不同文件大小和 repair 比例。
2. Synthetic optical benchmark：真实 QR 渲染 → 合成 2×2 图 → ZXing 解码 → CRC → FEC 重建，并注入符号丢失、连续帧丢失、模糊、透视、亮度和摩尔纹。
3. Physical benchmark：固定发送屏幕、接收手机、距离、角度、亮度和文件集，至少重复 5 次，报告中位数和 P10，而不是只报告最好一次。

## 4. 不建议直接照搬的部分

- 不直接用 RaptorQR 替换 libcimbar：两者是标准黑白 QR 与彩色矩阵两条不同物理编码路线，后者仍有更高密度潜力。
- 不把合成解析器的 106.2 FPS / 354 QR/s 当作相机能力；真实链路还包含曝光、滚动快门、屏幕刷新边界、缩放和透视。
- 不以 254.0 KB/s 小文件峰值作为版本目标；短文件会放大计时起点、首帧和 UI 更新误差。
- 不默认开放 6/8 QR；在手机摄像头画面里，码元像素密度和定位成功率可能比并行数更早成为瓶颈。
- 不沿用当前 60 帧实时队列思路。实时相机应优先最新帧并做有界 backpressure，积压旧帧只会增加延迟和重复画面。
- 不立即合并协议。V6 必须有独立 magic/version/codec 标识、严格 CRC 和完整文件 SHA-256，避免接收端把 V1–V4 或其他二维码误判为 RaptorQ 数据。

## 5. 建议实施路线

### Phase 0：基准口径与测试夹具（1–2 天）

交付物：`benchmark/` 测试工具、统一 JSON 结果、基线报告。

- 为 V5.2/V5.4 增加端到端计时定义，但先不改变传输协议。
- 固定测试文件：100 KB 随机、1 MB 随机、10 MB 随机、1 MB 文本、典型 JPEG/MP4。
- 记录原始大小、压缩后大小、唯一块、重复块、repair、完成时间、净 KB/s、CPU/内存和失败原因。
- 用现有 V5.2 B 模式重测 5 次，确认“180 KB/s 解码载荷率”对应的端到端中位数。

Go 条件：同一测试重复结果可稳定复现，所有版本使用相同计时边界。

### Phase 1：V6 单码 RaptorQ 协议原型（2–4 天）

交付物：本地网页发送/接收原型，不部署。

- 在独立目录建立 V6，接入 RaptorQ WASM、CRC32C、文件元数据和完整文件校验。
- 先用 V20-L、单 QR、15/20/30 FPS 打通发送、扫描、去重、恢复和下载。
- 建立 source-first / balanced / even-spread 调度 A/B。
- 验证离线首次加载、WASM 初始化失败提示和 100 MB 文件内存边界。

Go 条件：桌面合成测试 100% 重建；20% 随机符号丢失可恢复；协议错误和损坏包不会污染解码状态。

### Phase 2：2/4 QR 并行与流水线（3–5 天）

交付物：V6 高速实验版与 synthetic benchmark。

- 发送端加入 1/2/4 QR、V20/V30、ECC L/M 和 10–30 FPS 参数矩阵。
- 使用 Worker 池预渲染有限窗口，显示目标/实际 FPS、QR/s、缓存水位。
- 接收端一次解析多码；实时路径只保留最新待处理帧，过载时明确统计 busy drop。
- 加入 burst-loss 测试：不是只随机丢 20% QR，还要连续丢 1/2/4/8 个显示帧。

Go 条件：目标接收设备合成解析能力至少达到目标显示速率的 1.5 倍；持续运行 10 分钟无队列增长和明显内存爬升。

### Phase 3：iPhone 真机参数搜索（2–3 天）

交付物：可复现的参数推荐表与原始测试记录。

- 同一 iPhone、同一发送屏幕、固定支架和光照，对 V20/V30、2/4 QR、20/30 FPS、L/M、repair 10/20/30% 做矩阵测试。
- 每组至少 5 次，主指标为 6.5 MB 和 10 MB 随机文件的端到端净速率中位数；同时报告 P10、成功率和温升。
- 与 V5.2 B、V5.4 B 做同文件 A/B；压缩文件和不可压缩随机文件分开报告。

Go 条件：V6 在 10 MB 随机文件上成功率 ≥95%，中位净速率 ≥150 KiB/s，且 P10 ≥120 KiB/s；否则保留为实验通道，不进入默认推荐。

### Phase 4：自适应与产品整合（3–5 天）

交付物：可供普通用户使用的 V6 候选版。

- 根据检测到的实际发送 FPS、扫码 QR/s、连续丢帧和单码像素尺寸，在 4 QR → 2 QR → 1 QR 之间降档。
- repair 比例只在新会话或明确的反馈通道下调整，避免单向链路双方参数失配。
- UI 给出“标准 QR 高速版”和“彩色矩阵 iOS 高速版”的选择，不把两者混成一个模糊的自动模式。
- README 只发布同口径的端到端中位数；解码载荷率保留在诊断表。

## 6. 首轮任务拆分

| 优先级 | 任务 | 预期收益 | 主要风险 |
| --- | --- | --- | --- |
| P0 | 统一端到端计时与 JSON benchmark schema | 让 180 KB/s 与 183.6 KB/s 可以公平比较 | 需要明确 UI 点击和完成事件边界 |
| P0 | V6 协议标识、CRC32C、SHA-256、RaptorQ 单码闭环 | 验证核心复用价值 | WASM 体积、初始化和 Safari 内存 |
| P0 | 真实 QR 合成全链路 benchmark | 在真机前快速筛参数与回归 | 合成图不能替代真实摄像头 |
| P1 | 2×2 四码并行和多符号扫描 | 直接提高每个稳定曝光的载荷 | 单码尺寸缩小导致识别率下降 |
| P1 | Balanced repair 调度与 burst-loss 测试 | 提升漏扫和晚加入恢复 | repair 过高会压低净速率 |
| P1 | 有界最新帧 backpressure | 降低实时延迟与旧帧堆积 | 丢帧策略需要与显示 FPS 联调 |
| P2 | 发送端 Worker 池和预渲染窗口 | 稳定实际 FPS，减少主线程抖动 | 缓存与内存上限 |
| P2 | iPhone 参数矩阵和 V5.2/V5.4 A/B | 决定默认路线 | 测试时间和环境一致性 |
| P3 | 自适应并行度与推荐配置 | 改善普通用户成功率 | 单向链路缺少即时反向反馈 |

## 7. 最终成功标准

本次整合不以“代码用了 RaptorQ”作为成功，而以以下结果作为成功：

- 10 MB 不可压缩文件，iPhone 真机端到端成功率 ≥95%。
- 标准 QR V6 的端到端净速率中位数 ≥150 KiB/s，P10 ≥120 KiB/s。
- 若 V6 无法超过 V5.2/V5.4，则它仍可因标准 QR、浏览器接收和跨平台性保留，但不宣传为最高速版本。
- 所有速率都能由仓库内 benchmark JSON 和测试条件复现；README 不再混用理论速率、解码载荷率和端到端净速率。
- V1–V5.4 的现有协议与入口不受影响，V6 可独立回退或删除。

## 8. 推荐的第一步

先执行 Phase 0，不立即移植全部 RaptorQR UI。只有把 QRREC V5.2/V5.4 的真实端到端净速率测清楚，后面的“是否更快”才有可信基线。随后用独立 V6 单码原型验证 RaptorQ WASM，再进入四码并行；这条顺序能用最小改动尽早淘汰不适合 iPhone/Safari 的依赖或参数。
