# QRREC V6.0 音频方案：已停止的技术预研

> 状态：仅保留为历史技术预研，不实施。评估结论是消费设备音频的数据承载量相对光学通道过低，因此正式 V6 已改为 RaptorQ 标准 QR 高速版，见 `v6/README.md`。本文下方出现的“V6”均指当时的音频概念，不代表当前正式版本。

> 状态：技术方案与实验计划，不包含实现、发布或部署
> 日期：2026-08-02
> 当前基线：V5.1 原生 iOS 接收端，光学理论载荷约 146.5 KB/s，项目最新真机记录约 131.4 KB/s

## 1. 先给结论

V6 可以加入“发送端扬声器发声、接收端麦克风收取”的近超声数据通道，也可以让它与现有 libcimbar 光学通道同时运行。不过应把三个目标分开：

1. **音频独立传输可行**：适合令牌、文本、密钥、小配置和小文件；使用普通消费设备传大文件会非常慢。
2. **音频与光学同向叠加可行**：两路数据可以用同一会话 ID 和喷泉块编号汇入同一个重组器，但预计只给 V5.1 增加约 0.05%–0.4% 的直接吞吐量，肉眼几乎感受不到。
3. **最值得做的是光学主链路 + 音频反馈链路**：光学继续承担文件主体，接收端用声音反向发送“已连接、质量、建议帧率/模式、完成、停止”等控制消息。音频速度虽低，却足以承载控制数据，能减少无效发送并让链路自适应。

因此建议把 V6 定位为“**多模态协同传输实验版**”，而不是宣称“超声高速文件传输版”。第一阶段必须同时验证音频独立传输和同向叠加；若目标是实际体验提升，则最终默认策略应倾向“光学向前、音频反馈”。

## 2. 当前项目基线与可以复用的接口

V5.1 的光学链路为：

```text
网页文件输入
  -> zstd + Wirehair 喷泉码
  -> 每个 libcimbar 画面约 10 个 750 B 喷泉块
  -> 20 FPS 彩色矩阵
  -> iPhone 1080p/60 FPS AVFoundation
  -> 3 路 C++ libcimbar/OpenCV 解码
  -> 串行 Wirehair 重组
```

V6 不应复制第二套文件封装和完成逻辑。现有 libcimbar 喷泉块已经包含 6 B `FountainMetadata`，其中有文件/会话标识、文件大小和块编号；接收端 `fountain_decoder_sink` 可以逐块接收。因此音频通道可以复用同一数据层：

```text
                     -> 光学 PHY -> 750 B 喷泉块 -+
文件 -> 统一喷泉块调度器                              +-> 同一个 Wirehair 重组器
                     -> 音频 PHY -> 750 B 喷泉块 -+
```

关键约束是两种介质必须从同一个块调度器领取**不同的 block ID**。若光学编码器和音频编码器各自从零开始，会大量发送重复喷泉块，叠加就失去意义。

## 3. “超声波”在消费设备上的真实边界

### 3.1 可用频带

常见浏览器、电脑和手机音频链路使用 44.1 或 48 kHz 采样。根据奈奎斯特上限，可表示的最高频率只有 22.05 或 24 kHz；扬声器、麦克风、系统滤波和机身声学结构还会进一步压缩真正可用的高频带宽。

V6 应提供两个明确标注的档位，不能把二者都简单叫作“完全无声”：

| 档位 | 建议频带 | 特性 | 主要风险 |
| --- | --- | --- | --- |
| 严格超声实验 | 20.2–22.2 kHz | 尽量避开通常的人耳听域 | 可用带宽窄；部分设备几乎发不出或收不到；44.1 kHz 设备只剩很小余量 |
| 近超声兼容 | 18.5–21.5 kHz | 更容易在内置扬声器/麦克风上获得稳定 SNR | 年轻用户、儿童或宠物可能感知到高频；不能承诺“所有人都听不到” |

频带必须按运行时实际采样率裁切。若网页实际只有 44.1 kHz，严格超声上沿不得超过约 21.8 kHz，并要保留抗混叠保护带；若裁切后可用带宽不足，页面应直接判定该档不可用，而不是悄悄改成可闻频率。

已有消费硬件研究也大多使用“近超声”而不是任意高频：Google Nearby 相关协议使用 18.5–20 kHz；高数据率 NUSC 实验使用 18–20 kHz；BatNet 使用 20–24 kHz。

### 3.2 信息论上限

理想带限高斯信道容量为：

```text
C = B × log2(1 + SNR)
```

这只是理想上限，不包含前导、导频、循环前缀、纠错、交织、CRC、设备频响凹陷、多径、移动和丢包。

| 可用带宽 | 5 dB SNR | 10 dB SNR | 20 dB SNR |
| --- | ---: | ---: | ---: |
| 2 kHz（严格超声） | 4.1 kbit/s | 6.9 kbit/s | 13.3 kbit/s |
| 3 kHz（近超声） | 6.2 kbit/s | 10.4 kbit/s | 20.0 kbit/s |

真实净速率往往只占这一上限的一部分，而且高频 SNR 随距离、方向和设备型号剧烈变化。

### 3.3 公开系统的实测参照

| 系统/研究 | 设备与频带 | 报告速率 | 对 V6 的意义 |
| --- | --- | ---: | --- |
| [Google Nearby 近超声协议](https://getreuer.info/papers/getreuer2018ultrasonic/index.html) | 消费手机，18.5–20 kHz，DSSS | 94.5 bit/s 原始速率；2 m 可靠、常可到 10 m | 很稳健，但只适合令牌和控制消息 |
| [ggwave](https://github.com/ggerganov/ggwave) | 多平台 FSK + RS；超声档从约 15 kHz 起 | 8–16 B/s | 很适合作为最低成本可行性基线，不适合作为文件高速链路 |
| [BatNet](https://arxiv.org/abs/2008.00136) | 手机内置扬声器/麦克风，20–24 kHz，8-PSK | 超过 600 bit/s，最远约 6 m | “严格超声 + 手机内置硬件”的现实参考 |
| [High Data Rate NUSC](https://eurasip.org/Proceedings/Eusipco/Eusipco2021/pdfs/0001681.pdf) | 消费笔记本，18–20 kHz，相干调制 + 自适应均衡 | 4 kbit/s，最远约 5 m | V6 近超声高性能档的合理拉伸目标 |
| [UltraComm](https://eudl.eu/pdf/10.1007/978-3-030-38819-5_12) | 专门超声换能器/信号发生器，接收端手机 | 最高 16.24 kbit/s | 是实验室上限参照，不能作为普通设备承诺 |

据此，V6 应采用以下工程目标：

| 级别 | 净数据目标 | 1 MiB 理想传输时间 | 用途 |
| --- | ---: | ---: | --- |
| P0 稳健基线 | 8–16 B/s | 18–36 小时 | 会话发现、控制包、验证端到端音频链路 |
| P1 严格超声 | 50–100 B/s | 2.9–5.8 小时 | 小文本、令牌、反馈、极小文件 |
| P2 近超声实用 | 125–250 B/s | 70–140 分钟 | 小配置、缩略信息、同向补充块 |
| P3 实验拉伸 | 375–500 B/s | 35–47 分钟 | 近距离、固定设备、安静环境下的小文件实验 |

这里的 1 MiB 时间尚未加入连接训练、喷泉冗余和失败重试。音频单独传大文件可以作为实验，但不应成为 V6 的主产品叙述。

## 4. 音频与二维码/彩色矩阵能否“叠加”

### 4.1 同时传输在系统上可行

屏幕/摄像头与扬声器/麦克风是独立物理介质。网页发送端可以在播放 libcimbar 帧的同时由 `AudioWorklet` 输出 PCM；iOS App 可以在视频采集和 C++ 图像解码的同时采集 48 kHz 单声道音频并进行 DSP。

两路不需要帧级同步，只需要共享：

- `session_id / encode_id`
- 文件长度与文件哈希
- 喷泉块大小和编码参数
- 全局唯一 block ID
- 每个音频包自己的版本、长度和 CRC

这样任意一路丢失都不会阻塞另一通道，接收端只把通过 CRC 的新块送入同一个 Wirehair 解码器。

### 4.2 直接速度叠加几乎没有收益

以 V5.1 最新真机记录 131.4 KB/s 为基线：

- 音频净速 75 B/s，只增加约 **0.056%**。
- 音频净速 250 B/s，只增加约 **0.19%**。
- 音频净速 500 B/s，只增加约 **0.37%**。
- 即使达到需要特殊实验条件的 16.24 kbit/s 原始速率，也只相当于约 2.0 KB/s，低于 V5.1 的 2%。

所以“光学 131.4 KB/s + 音频 0.1–0.5 KB/s”在数学上能相加，在体验上不会形成明显提速。若要让音频真正提高完成率，价值主要来自：

- 光学短暂失焦或遮挡时，音频仍持续提供独立喷泉块；
- 音频反向报告接收质量，让发送端自动调整光学 FPS、模式和亮度提示；
- 接收完成后通过音频通知发送端停止，避免无限播放；
- 用音频发送会话信息、文件哈希或加密协商，减轻光学首帧依赖。

### 4.3 三种候选组合

| 方案 | 数据方向 | 优点 | 缺点 | 建议 |
| --- | --- | --- | --- | --- |
| A. 音频独立 | 发送端扬声器 -> 接收端麦克风 | 完整验证声音传数据；不依赖摄像头 | 大文件极慢 | V6 必做实验模式 |
| B. 光学 + 音频同向 | 屏幕和扬声器同时 -> iPhone | 架构直观；遮挡时仍有极慢备份 | 对净速度贡献不足 0.4%；同时占用两种传感器 | 做实验和指标对照，不作为默认卖点 |
| C. 光学向前 + 音频反向 | 屏幕 -> 相机；iPhone 扬声器 -> 发送端麦克风 | 低速带宽与控制消息大小匹配；可闭环自适应和自动停止 | 发送网页需要麦克风权限；方向改变后要处理回声/碰撞策略 | **最推荐的最终默认协同模式** |

## 5. V6 音频 PHY 设计

### 5.1 分阶段调制策略

不建议第一版直接投入复杂 OFDM。先建立能测量、能回放、能复现的基线，再逐步提高频谱效率。

#### P0：FSK/ggwave 基线

- 复用 ggwave 的 MIT 许可 C/C++ 核心、WebAssembly 发送示例和 iOS 音频示例思路。
- 只传 16–64 B 测试包：协议版本、会话 ID、序号、时间戳、随机 payload、CRC。
- 用于完成设备频响扫描、实时谱图、误包率、距离和方向测试。
- ggwave 超声档约从 15 kHz 起，不能直接当作“严格超声完成版”；它只是最低风险的链路基线。

#### P1：单载波 8-PSK 严格超声

- 频带优先 20.2–22.2 kHz，48 kHz 采样。
- 线性调频 chirp 前导 + 载波/符号定时恢复。
- 8-PSK 或稳健 BPSK/QPSK 档位，卷积码或 Reed–Solomon，块交织，CRC32C。
- 先以 300–800 bit/s 原始速率为目标，固定设备和 0.5–2 m 环境验证后再提高。

#### P2：自适应 OFDM 近超声

- 18.5–21.5 kHz 内划分子载波，保留 DC/边缘保护和导频。
- 每次启动先发送 300–500 ms 训练序列，测量各频点 SNR；差的子载波关闭，好的子载波在 BPSK/QPSK 间选择。
- 加循环前缀抵抗室内多径，用导频跟踪相位漂移，用频域均衡补偿扬声器和麦克风的频响。
- 限制 OFDM 峰均比，避免扬声器非线性产生可听互调和削波。
- 目标净速 1–2 kbit/s，实验拉伸到 3–4 kbit/s；若必须退到 18.5–20 kHz，UI 必须提示这是“近超声”，不是对所有人都不可闻。

### 5.2 音频包格式建议

```text
Preamble / Sync
Version          1 B
Packet type      1 B   DATA / HELLO / QUALITY / COMPLETE / STOP
Session ID       4 B
Sequence ID      2 B
Payload length   2 B
Payload          N B
CRC32C           4 B
FEC / interleave outside the logical packet
```

数据包的 payload 有两种模式：

- **控制模式**：直接承载小消息；一包控制在 16–48 B 内。
- **喷泉模式**：承载一个完整的 libcimbar 750 B 喷泉块。音频速率低时，一个块可能跨多个 PHY frame，但交给 Wirehair 前必须完整通过 CRC。

V6 首版不改变 V5/V5.1 码流；新协议只存在于独立的 `/v6/` 发送端与 V6 iOS App 中。

## 6. 抗环境音与设备差异策略

“使用高频”本身不能保证抗干扰。环境音乐、钥匙碰撞、摩擦声、部分电器和其他设备的超声信号都可能进入目标频带。V6 应同时采用以下策略：

1. **原始采集优先**：iOS 使用 `AVAudioSession` 的 `.measurement` 模式，尽量减少系统 DSP。Apple 明确说明该模式用于尽量降低系统提供的输入/输出信号处理。网页接收时请求关闭 `echoCancellation`、`noiseSuppression` 和 `autoGainControl`，并读取实际 track settings，不能假定浏览器接受了请求。
2. **带通 + 自适应陷波**：只处理目标频带；训练阶段识别持续窄带干扰并关闭对应载波。
3. **相关前导**：用 chirp 或伪随机序列做包检测，不以单个能量阈值判断“收到数据”。
4. **导频和均衡**：持续估计频率选择性衰减、相位漂移和设备移动。
5. **交织 + FEC + CRC**：交织对付突发噪声，FEC 修复有限错误，CRC 防止错误块进入文件重组器。
6. **包级去重**：按 session + sequence/block ID 丢弃重复音频包。
7. **自动降级**：PER 连续超标时依次降调制阶数、增加 FEC、减少子载波、切换频带；失败时关闭音频数据通道，但不能拖累光学通道。
8. **幅度和可闻性控制**：启动前做短校准，限制输出增益和峰值；提供一键静音。不能用“用户听不见”代替声压和失真检查。

Apple 侧参考：[Measurement mode](https://developer.apple.com/documentation/avfaudio/avaudiosession/mode-swift.struct/measurement)；Web 侧参考：[MediaTrackConstraints](https://developer.mozilla.org/en-US/docs/Web/API/MediaTrackConstraints) 与 [AudioWorklet](https://developer.mozilla.org/en-US/docs/Web/API/Web_Audio_API/Using_AudioWorklet)。

## 7. 工程架构规划

### 7.1 网页发送端

新增建议目录：

```text
v6/send/
  index.html
  main.ts
  audio-worklet.ts
  audio-modem.ts
  hybrid-scheduler.ts
```

职责：

- 保留 V5.1 libcimbar 20 FPS 编码与模式选择。
- 用户点击“开始”后显式启动 `AudioContext`，检查实际 sample rate。
- `AudioWorklet` 只负责实时样本生成；编码、FEC 和喷泉块调度放到 Worker，避免 UI/二维码绘制抖动影响音频。
- 显示实际采样率、频带、音频档位、发送 bit/s、音频块编号、光学块编号和两路总进度估算。
- 若启用推荐的反向反馈模式，再请求发送端麦克风权限；未授权时光学发送仍正常工作。

### 7.2 iOS 接收端

新增独立工程或 target：

```text
v6/ios/OpticalReceiverV6/
  App/
  Audio/AudioCapture.swift
  Audio/AudioDemodulator.cpp
  Audio/AudioProtocol.cpp
  Hybrid/HybridPacketRouter.cpp
```

职责：

- 保留 V5.1 的 AVFoundation 视频采集和 3 路 C++ 图像解码。
- 在 `Info.plist` 增加麦克风用途说明，权限被拒绝时自动回退为纯光学模式。
- 音频以 48 kHz、单声道、Float32/PCM 进入无锁环形缓冲区；DSP 不在实时回调里做文件重组或 UI 更新。
- Accelerate/vDSP 或 C++ FFT 工作线程负责带通、相关、解调、FEC 和 CRC。
- `HybridPacketRouter` 对光学和音频来源统一去重，再把有效 750 B 喷泉块送入现有串行 Wirehair sink。
- UI 分开显示光学有效帧率、音频 SNR/PER/bit/s、两路独立新块数、重复块数和完成贡献；不能只显示相加后的漂亮数字。
- 若做反馈模式，App 用短控制包播报质量与完成状态；与同向音频数据同时启用时使用时分窗口避免双向超声互撞。

### 7.3 音视频采集并存验证

当前 V5.1 的 `AVCaptureSession` 只处理视频。V6 PoC 可先让视频继续走 `AVCaptureSession`，麦克风走 `AVAudioEngine`；统一配置 App 的 `AVAudioSession`。若出现资源争用、路由变化或采样率被系统改写，再改为同一 `AVCaptureSession` 增加 `AVCaptureAudioDataOutput`。

所有情况下都要记录实际采样率和路由，不能只记录请求值。Apple 的硬件设置文档也要求在激活 session 后查询实际配置：[Configuring Device Hardware](https://developer.apple.com/library/archive/documentation/Audio/Conceptual/AudioSessionProgrammingGuide/OptimizingForDeviceHardware/OptimizingForDeviceHardware.html)。

### 7.4 对当前代码的落点

| 当前文件/模块 | V6 规划改动 |
| --- | --- |
| `v5/send/main.ts` 与 `v5/send/runtime-send.html` | 复制为独立 V6 发送入口；保留现有 libcimbar 运行层，接入音频启动、模式选择和统一调度器 |
| `v5.1/native/libcimbar/src/lib/cimbar_js/cimbar_js.cpp` | 增加受控的“领取下一个原始喷泉块”接口，避免光学和音频各自生成重复 block ID |
| `v5.1/native/libcimbar/src/lib/fountain/FountainMetadata.h` | 保持现有 6 B 元数据兼容；V6 外层音频包再增加版本、长度与 CRC |
| `v5.1/native/libcimbar/src/lib/fountain/fountain_decoder_sink.h` | 继续作为唯一文件重组入口；通过 `HybridPacketRouter` 串行喂入两种介质的有效新块 |
| `v5.1/ios/OpticalReceiverV51/App/CimbarReceiverWebView.swift` | 复制到 V6 工程后保留视频链路，旁路接入音频采集、DSP 指标和统一完成状态 |
| `package.json` 与 Vite 配置 | 新增本地 `dev:sender:v6`、`build:sender:v6`；规划阶段不新增部署或线上路由 |

## 8. 开源复用决策

| 项目 | 许可证/状态 | 可复用内容 | 决策 |
| --- | --- | --- | --- |
| [ggwave](https://github.com/ggerganov/ggwave) | MIT；C/C++、WASM、iOS 示例，仍有较完整生态 | FSK/RS 基线、音频 I/O 接法、离线波形测试 | **P0 采用或做薄封装**；最终高性能 PHY 不受其 8–16 B/s 限制 |
| [quiet/quiet](https://github.com/quiet/quiet) / [quiet-js](https://github.com/quiet/quiet-js) | BSD-3-Clause；依赖 liquid-dsp，推荐 FEC 另有 LGPL；浏览器兼容信息明显陈旧 | SDR profile 思路、OFDM/GMSK 参数参考 | 不直接集成；依赖、体积、维护和旧 Web 接口风险过高 |
| [BatNet](https://github.com/zarandya/BatNet) | GPL-3.0；Android 工程，提交很少 | 20–24 kHz 8-PSK 参数和论文实验方法 | 只参考论文与测试方法，不复制代码，避免许可证和平台不匹配 |
| V5.1 内置 libcimbar | MPL-2.0 独立模块，当前项目已集成 | Wirehair 750 B 喷泉块、文件元数据、重组器 | **继续作为统一数据层**；V6 自有胶水代码保持与现有许可证边界一致 |

## 9. 分阶段实施与验收

### 阶段 0：离线声学探针（1 个里程碑）

实现内容：

- 网页生成 chirp、多音和 ggwave 测试 WAV/PCM。
- iOS 录制原始 48 kHz PCM，并保存频谱、设备型号、距离、方向和音量。
- 建立 18–24 kHz 的发送/接收频响图。

通过条件：

- 至少一台电脑 -> 当前目标 iPhone 在 0.5 m、1 m、2 m 都能稳定检测训练序列。
- 可区分“硬件没有频带”“SNR 不足”“解调失败”。
- 严格超声和近超声分别记录，不混为一个结果。

### 阶段 1：音频独立数据模式

实现内容：

- P0 FSK 基线和逻辑包协议。
- 传输 32 B、1 KiB、16 KiB 三档数据，完成 CRC/哈希校验和保存。
- 加入带通、相关前导、去重、FEC、实时 SNR/PER/净速率。

通过条件：

- 安静室内 1 m 固定设备，连续 1000 个短包 PER <= 1%。
- 1 KiB 数据至少连续完成 20 次且哈希全部一致。
- 环境音乐/说话条件下不产生“校验通过的错误文件”。允许变慢或不完成，不允许静默损坏。

### 阶段 2：光学 + 音频同向叠加

实现内容：

- 从同一 fountain scheduler 分配全局唯一块 ID。
- 光学和音频有效块进入同一 Wirehair sink。
- 增加 optical-only、audio-only、hybrid 三种 A/B 模式。

通过条件：

- 同一文件三种模式均能得到同一哈希。
- hybrid 不得让光学有效速率下降超过 5%。
- UI 能证明最终重组中分别有多少**唯一块**来自两种介质。
- 若音频贡献小于 0.1% 或明显拖累电量/CPU，保留实验开关但不设为默认。

### 阶段 3：音频反馈闭环

实现内容：

- 接收端发送 `HELLO / QUALITY / COMPLETE / STOP`。
- 发送网页接收反馈，自动在 B/Bm/Bu、15/20 FPS 间调节。
- 发送端收到完成确认后停止光学和音频输出。

通过条件：

- `COMPLETE` 在接收完成后 2 秒内到达发送端的成功率 >= 95%。
- 自动调节在低光、手持和轻微遮挡场景下，端到端完成时间优于固定 20 FPS，或失败率显著下降。
- 反馈通道失败时不影响单向光学传输完成。

### 阶段 4：自适应 OFDM（仅在前面数据证明值得后）

进入条件：

- P1 的设备频响证明至少有连续 2 kHz 可用带宽。
- 音频净速已成为明确瓶颈，并且控制/叠加实验确实带来产品价值。

目标：

- 近超声 1–2 kbit/s 净速为通过线，3–4 kbit/s 为拉伸目标。
- 不以专门换能器的 16.24 kbit/s 作为普通手机验收标准。

## 10. 真机测试矩阵

每个测试必须保存原始 PCM 或至少保存可复算的频谱/统计日志。

| 维度 | 测试档位 |
| --- | --- |
| 距离 | 0.3 m / 0.5 m / 1 m / 2 m / 5 m |
| 角度 | 正对 / 30° / 60° / 背对 |
| 环境 | 安静 / 人声 / 音乐 / 电视 / 风扇 / 键盘与钥匙声 |
| 运动 | 固定 / 手持轻微移动 / 短时遮挡 |
| 频带 | 20.2–22.2 kHz / 18.5–21.5 kHz |
| 声量 | 25% / 50% / 75%，不默认满音量 |
| 模式 | audio-only / optical-only / 同向 hybrid / 反馈 hybrid |
| 文件 | 32 B / 1 KiB / 16 KiB / 1 MiB；音频大文件只做限时实验 |

核心指标：

- 训练成功率、锁定时间、SNR、BER、PER、CRC 失败数；
- 原始 bit/s、FEC 后净 bit/s、每焦耳有效字节；
- 光学独立有效 KB/s、音频独立有效 B/s、两路唯一喷泉块贡献；
- 文件完成时间与 SHA-256；
- CPU、内存、温度、电量和音频路由变化；
- 可闻性反馈与是否出现点击声、拍频或互调杂音。

## 11. Go / No-Go 决策门槛

### 继续投入同向数据叠加

满足以下任一条件：

- 音频让高遮挡场景的完成率提高至少 10 个百分点；
- 音频提供的独立块使 P95 完成时间下降至少 5%；
- 音频独立模式被确认对小令牌/配置传输有独立产品价值。

### 只保留反馈通道

- 音频净速不足 125 B/s，但 16–48 B 控制包在 2 秒内可靠到达；或
- 同向叠加收益小于 1%，但自动停止和自适应明显改善体验。

### 暂停音频方案

- 目标设备在严格超声带无法稳定形成连续可用频带；
- 必须退到明显可闻频率才能可靠工作，而用户目标坚持完全不可闻；
- 同时采集导致 V5.1 光学有效速率下降超过 5%，且无法通过线程/音频会话调整解决；
- 声音失真、可闻性、发热或耗电风险高于所得收益。

## 12. 最终推荐的 V6 产品定义

V6 首次实现时保留三个清晰入口：

1. **声音实验**：只用音频传小数据，显示真实 bit/s、预计完成时间和频带，不隐藏速度限制。
2. **双通道实验**：屏幕和声音同向发送，同一文件汇入同一喷泉重组器，用数据验证音频到底贡献了多少。
3. **智能协同（候选默认）**：文件主要走 V5.1 光学链路，声音主要做反向质量反馈、自动调参和完成确认。

V6 的成功标准不是把两个理论速率简单相加，而是证明：在不降低 V5.1 光学性能的前提下，声音能否让连接、完成确认、弱环境恢复或小数据传输中的至少一项获得可测量的改善。
